# Installing and Updating Tools

sudo apt update && apt upgrade, why use && here

apt install cron-daemon-common -> brief about it.&#x20;

sudo apt install \<software name>&#x20;

git clone \<github clone url>&#x20;

