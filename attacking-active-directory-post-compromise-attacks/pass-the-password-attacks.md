# Pass the Password Attacks

Run the "crackmapexec" command to check whether crackmapexec exists on the attacking PC.&#x20;

start both Windows Server and 10 Enterprise.&#x20;

Run "crackmapexec smb WIN\_10\_IP -u USERNAME -d DOMAIN\_NAME -p PASSWORD"

"cmedb"

"hosts" to see all host

"creds" to see the credentials
